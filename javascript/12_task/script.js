const user = {
    username: "jaimin",
    password: "12345",
    email: "testgmail.com",
};

function validata(user){
    const error =[]

    if(user.username.length < 4)
        error += ("4 char")


}
